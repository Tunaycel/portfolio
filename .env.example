# Public canonical origin. Set to the final HTTPS domain before production build.
SITE_URL=
# Optional contact delivery. Without these, the website shows a direct email link.
RESEND_API_KEY=
# Use a sender on a domain verified in your Resend account.
CONTACT_FROM=
CONTACT_TO=h.tunaycelik@gmail.com
# Enable only behind an edge that overwrites x-forwarded-for; also configure edge rate limits.
TRUST_PROXY=false
